objectFitVideos();

// Select the video element by its ID
const video = document.getElementById('myVideo');

// Enable autoplay
video.autoplay = true;

// Load the video to apply autoplay
video.load();
